{
  "name": "skryb-runtime",
  "private": true,
  "version": "0.0.0",
  "description": "Skryb portable HTML document runtime",
  "scripts": {
    "build": "node scripts/build.mjs",
    "check": "tsc --noEmit",
    "test": "npm run build && node --test test/render-runtime.test.js"
  },
  "devDependencies": {
    "esbuild": "0.28.2",
    "typescript": "5.9.3"
  }
}
